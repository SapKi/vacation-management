{
  "permissions": {
    "allow": [
      "PowerShell(Test-NetConnection -ComputerName localhost -Port 5173 -InformationLevel Quiet -WarningAction SilentlyContinue)",
      "PowerShell($r = Invoke-RestMethod -Uri \"http://localhost:3000/api/vacation-requests\" -ErrorAction Stop; \"Requests count: $\\($r.Count\\)\"; $r | Select-Object id, status, start_date, end_date | Format-Table)"
    ]
  }
}
